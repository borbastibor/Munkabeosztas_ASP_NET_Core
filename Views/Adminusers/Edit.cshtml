@model Munkabeosztas_ASP_NET_Core.Models.Adminuser

@{
    ViewData["Title"] = "Admin felhasználó szerkesztése";
}

@await Html.PartialAsync("_AdminMenuPartial")

<div class="adminform-container">
    <div class="adminform-title">
        @ViewData["Title"]
        @Html.ActionLink("[X]", "Index", "Adminusers", null, new { @class = "cancel-button" })
    </div>
    <div class="adminform-body">
        @using (Html.BeginForm("Edit", "Adminusers", FormMethod.Post))
        {
            @Html.ValidationSummary(false, "", new { @class = "text-danger" })

            @Html.LabelFor(model => model.Username, new { @class = "labeltext" })<br />
            @Html.TextBoxFor(model => model.Username, new { @class = "textbox" })<br /><br />

            @Html.HiddenFor(model => model.AdminuserId)

            <input type="submit" class="save-submit-button" name="Save" value="Mentés" class="btn-submit" /><br />
        }
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}